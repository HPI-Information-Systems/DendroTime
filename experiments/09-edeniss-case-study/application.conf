include required(file("../common"))

# overwrites
dendrotime {
    data-path = datasets
    ground-truth-path = ground-truth
    results-path = results
    max-workers = 2
    progress-indicators {
        hierarchy-similarity = labelChangesAt
        hierarchy-quality = weightedHierarchySimilarity
        cluster-quality = null
    }
}
