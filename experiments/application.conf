dendrotime {
    data-path = data/datasets
    ground-truth-path = data/ground-truth
    results-path = results
    store-results = yes
    max-workers = 10
    reporting-interval = 15m
}

akka {
    loglevel = WARNING
}
